services:
  webserver:
    image: "elbromasmc/alc-rimac"
    restart: unless-stopped
    build:
      context: ../src
      dockerfile: ./Dockerfile
      target: development
  db:
    restart: unless-stopped

